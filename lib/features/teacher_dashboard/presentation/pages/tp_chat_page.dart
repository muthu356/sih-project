import 'package:flutter/material.dart';

class TPChatPage extends StatelessWidget {
  const TPChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chat')),
      body: const Center(
        child: Text('Teacher-Parent Chat Module - Coming Soon'),
      ),
    );
  }
}
