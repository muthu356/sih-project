import 'package:flutter/material.dart';

class StudentsManagePage extends StatelessWidget {
  const StudentsManagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Manage Students')),
      body: const Center(
        child: Text('Students Management Module - Coming Soon'),
      ),
    );
  }
}
