import 'package:flutter/material.dart';

class ParentConnectPage extends StatelessWidget {
  const ParentConnectPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parent Connect')),
      body: const Center(
        child: Text('Parent Connectivity Module - Coming Soon'),
      ),
    );
  }
}
