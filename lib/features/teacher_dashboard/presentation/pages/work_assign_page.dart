import 'package:flutter/material.dart';

class WorkAssignPage extends StatelessWidget {
  const WorkAssignPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Assign Work')),
      body: const Center(
        child: Text('Work Assign Module - Coming Soon'),
      ),
    );
  }
}
